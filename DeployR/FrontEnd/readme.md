# Front End Dev for DeployR (2PintLabs - NOT OFFICIAL)

Here is where i'm building some front ends using PowerShell for my own use, but willing to share.
I have one that is fully PowerShell with all options baked into the code.

I have another one I'm building that will be easier to share, where the Logo is a separate file, and the menus are driven by the JSON file.  If you're looking to borrow one, I'd start there, and you can then update the JSON.

They are created to run outside of DeployR, so you can see what variables will be created.  Once run inside of DeployR, it will create the DeployR Variables which you'd see in the DeployR.log file.

I'll go into more detail in the readme for the JSON driven Frontend folder.